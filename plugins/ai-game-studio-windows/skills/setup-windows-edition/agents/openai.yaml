interface:
  display_name: "Set up the Windows edition"
  short_description: "Plan a native Windows game toolchain safely"
  default_prompt: "Use $ai-game-studio-windows:setup-windows-edition to inspect this Windows game project and propose one native, reversible setup."
policy:
  allow_implicit_invocation: false
